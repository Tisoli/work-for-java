
// 1. Construir una clase ArrayReales que declare un atributo de tipo double[] y que
// implemente una interfaz llamada Estadisticas. El contenido de esta interfaz es el siguiente:
// public interface Estadisticas {
//      double minimo();
//      double maximo();
//      double sumatorio();
// }
// 2. Codifica la interfaz **Forma** con el método **getArea()**. Crea tres clases **Rectangulo**, **Circulo** y **Triangulo** que implementen la interfaz **Forma**. Implementa el método **getArea()** para cada una de las tres clases.
// 3. Codifica interfaz **Animal** con un método llamado **ladrar()** que no reciba argumentos y no devuelva ningún valor (void). Crea una clase **Perro** que implemente **Animal** y sobrescriba **ladrar()** para que imprima "El perro está ladrando".

public interface Estadisticas {

    double minimo();
    double maximo();
    double sumatorio();

}
